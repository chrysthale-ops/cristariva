/* CRISTARIVA — intégration du Tarot divinatoire comme domaine de tirage. */
(function(){
'use strict';
if(!window.TAROT_DATA||!Array.isArray(window.TAROT_DATA.main))return;
const TAROT_DOMAIN='Tarot divinatoire';
const TAROT_NAMES_FR={1:'Le Passage Secret',2:'La Flamme Retrouvée',3:'Le Messager de Minuit',4:'Les Deux Chemins',5:'Le Masque qui Tombe',6:'La Promesse',7:'L’Éclipse',8:'Le Fil Invisible',9:'La Porte Ouverte',10:'Le Retour'};
const TAROT_NAMES_EN={1:'The Secret Passage',2:'The Rekindled Flame',3:'The Midnight Messenger',4:'The Two Paths',5:'The Falling Mask',6:'The Promise',7:'The Eclipse',8:'The Invisible Thread',9:'The Open Door',10:'The Return'};
function fixTarotCard(card){if(!card||card.oracle!=='tarot')return card;const id=Number(card.id);card.name=TAROT_NAMES_FR[id]||card.name;if(card.en)card.en.name=TAROT_NAMES_EN[id]||card.en.name;const ext=id<=8?'webp':'svg';const path=`./cards/tarot/${String(id).padStart(3,'0')}.${ext}?v=20260922e`;card.image=path;card.imageEn=path;return card;}
(window.TAROT_DATA.main||[]).forEach(fixTarotCard);(window.TAROT_DATA.all||[]).forEach(fixTarotCard);
try{if(typeof window.cr363Title==='function'){const previousCr363Title=window.cr363Title;window.cr363Title=function(card){if(card?.oracle==='tarot'){fixTarotCard(card);return state?.lang==='en'?(card.en?.name||card.name):card.name;}return previousCr363Title(card);};}if(typeof window.cardName==='function'){const previousCardName=window.cardName;window.cardName=function(card){if(card?.oracle==='tarot'){fixTarotCard(card);return state?.lang==='en'?(card.en?.name||card.name):card.name;}return previousCardName(card);};}}catch(e){}
const domainSelect=document.querySelector('#domain');
const isTarot=()=>typeof state==='object'&&state&&state.domain===TAROT_DOMAIN;
const esc=value=>typeof readingEscape==='function'?readingEscape(value):String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));
try{if(typeof DOMAIN_EN==='object')DOMAIN_EN[TAROT_DOMAIN]='Divinatory Tarot';}catch(e){}
function ensureDomainOption(){if(!domainSelect)return;let o=[...domainSelect.options].find(x=>x.value===TAROT_DOMAIN);if(!o){o=document.createElement('option');o.value=TAROT_DOMAIN;domainSelect.appendChild(o)}o.textContent=state?.lang==='en'?'Divinatory Tarot':TAROT_DOMAIN}
function ensureContext(){if(!domainSelect)return null;let c=document.querySelector('#tarotContext');if(!c){c=document.createElement('p');c.id='tarotContext';c.className='muted';c.style.marginTop='8px';c.setAttribute('aria-live','polite');domainSelect.closest('div')?.appendChild(c)}return c}
function updateContext(){ensureDomainOption();const c=ensureContext();if(!c)return;c.hidden=!isTarot();c.innerHTML=state?.lang==='en'?'<b>Active deck: CRISTARIVA Divinatory Tarot</b><br>10 original cards · Relationship, timing, astrology and final synthesis remain available.':'<b>Jeu actif : Tarot divinatoire CRISTARIVA</b><br>10 cartes originales · Relation, datation, astrologie et synthèse finale restent disponibles.'}
function clearComplementaryCards(){state.relation=null;state.date=null;const r=document.querySelector('#relationResult'),d=document.querySelector('#dateResult');if(r)r.innerHTML='';if(d)d.innerHTML=''}
ensureDomainOption();ensureContext();updateContext();
if(document.readyState==='loading'){document.addEventListener('DOMContentLoaded',()=>{ensureDomainOption();updateContext();renderTarotCatalog()},{once:true})}
let previousDomain=state?.domain||domainSelect?.value||'Relations';
domainSelect?.addEventListener('change',()=>{const next=domainSelect.value;const switched=previousDomain===TAROT_DOMAIN||next===TAROT_DOMAIN;state.domain=next;if(switched&&previousDomain!==next)clearComplementaryCards();previousDomain=next;updateContext()});
function localizedTarotReading(card,en=false){fixTarotCard(card);const local=en?(card?.en||{}):(card||{});let scope='spirit';try{if(typeof cr51Scope==='function')scope=cr51Scope()}catch(e){}if(scope==='work')return local.reading_professionnel||local.meaning||local.definition||'';if(scope==='relation')return local.reading_relationnel||local.meaning||local.definition||'';return local.reading_spirituel||local.reading_tarot||local.meaning||local.definition||''}
if(typeof domainReading==='function'){const old=domainReading;domainReading=function(card){if(card?.oracle==='tarot'){fixTarotCard(card);return card.reading_tarot||card.meaning||card.definition||'';}return old(card)}}
if(typeof preciseReading==='function'){const old=preciseReading;preciseReading=function(card,focus,en){if(card?.oracle==='tarot')return localizedTarotReading(card,Boolean(en));return old(card,focus,en)}}
document.querySelector('#drawBtn')?.addEventListener('click',event=>{if(domainSelect)state.domain=domainSelect.value;if(!isTarot())return;event.stopImmediatePropagation();event.preventDefault();state.question=document.querySelector('#question').value.trim();const count=parseInt(state.format||1,10);state.draw=rand(window.TAROT_DATA.main,count).map(fixTarotCard);clearComplementaryCards();const positions=(state.lang==='en'?POSITIONS_EN:POSITIONS_FR)[count].map(x=>x[0]);document.querySelector('#drawCards').innerHTML=state.draw.map((card,i)=>cardHTML(card,positions[i])).join('');document.querySelector('#reading').innerHTML=interpretation(state.draw);document.querySelector('#results').classList.remove('hidden');document.querySelector('#deepening').classList.remove('hidden');updateContext();document.querySelector('#results').scrollIntoView({behavior:'smooth',block:'start'})},true);
if(typeof applyLanguage==='function'){const old=applyLanguage;applyLanguage=function(){old();(window.TAROT_DATA.main||[]).forEach(fixTarotCard);ensureDomainOption();const o=[...domainSelect.options].find(x=>x.value===TAROT_DOMAIN);if(o)o.textContent=state.lang==='en'?'Divinatory Tarot':TAROT_DOMAIN;updateContext();renderTarotCatalog()}}
const main=document.querySelector('main');
if(main&&!document.querySelector('#tarot-divinatoire-catalog'))main.insertAdjacentHTML('beforeend',`<section class="shell panel" id="tarot-divinatoire-catalog"><div class="section-title"><span class="eyebrow" id="tarotCatalogEyebrow">Tarot divinatoire CRISTARIVA</span><h2 id="tarotCatalogTitle">Les 10 cartes du Tarot divinatoire</h2><p class="muted" id="tarotCatalogIntro">Cliquez sur une carte pour consulter sa définition et son message essentiel.</p></div><div class="catalog-tools"><label class="sr-only" for="tarotCatalogSearch" id="tarotCatalogSearchLabel">Rechercher une carte du Tarot divinatoire</label><input autocomplete="off" id="tarotCatalogSearch" placeholder="Rechercher dans le Tarot divinatoire…" type="search"/></div><p aria-live="polite" class="muted" id="tarotCatalogCount"></p><div class="cards catalog-grid" id="tarotCatalogGrid"></div></section>`);
function renderTarotCatalog(){const search=document.querySelector('#tarotCatalogSearch'),grid=document.querySelector('#tarotCatalogGrid'),count=document.querySelector('#tarotCatalogCount');if(!search||!grid||!count)return;const en=state?.lang==='en',q=search.value.trim().toLocaleLowerCase(en?'en':'fr');const cards=window.TAROT_DATA.main.map(fixTarotCard).filter(card=>{const l=en?(card.en||{}):card;return !q||String(l.name||'').toLocaleLowerCase().includes(q)||String(l.keywords||'').toLocaleLowerCase().includes(q)||String(card.id).includes(q)});const title=document.querySelector('#tarotCatalogTitle'),intro=document.querySelector('#tarotCatalogIntro'),label=document.querySelector('#tarotCatalogSearchLabel');if(title)title.textContent=en?'The 10 Divinatory Tarot cards':'Les 10 cartes du Tarot divinatoire';if(intro)intro.textContent=en?'Select a card to read its definition and essential message.':'Cliquez sur une carte pour consulter sa définition et son message essentiel.';if(label)label.textContent=en?'Search the Divinatory Tarot':'Rechercher une carte du Tarot divinatoire';search.placeholder=en?'Search the Divinatory Tarot…':'Rechercher dans le Tarot divinatoire…';count.textContent=en?`${cards.length} / 10 cards displayed`:`${cards.length} / 10 cartes affichées`;grid.innerHTML=cards.map(card=>{const name=esc(en?(card.en?.name||card.name):card.name);return `<button type="button" class="catalog-item" data-tarot-card-id="${card.id}" aria-label="${esc(en?'View card':'Voir la carte')} : ${name}"><span class="catalog-image"><img src="${esc(cardImage(card))}" alt="${name}" loading="lazy"></span><span class="catalog-name">${String(card.id).padStart(2,'0')} · ${name}</span></button>`}).join('')||(en?'<p class="muted">No card found.</p>':'<p class="muted">Aucune carte trouvée.</p>')}
document.querySelector('#tarotCatalogSearch')?.addEventListener('input',renderTarotCatalog);
document.querySelector('#tarotCatalogGrid')?.addEventListener('click',event=>{const b=event.target.closest('[data-tarot-card-id]');if(!b)return;const card=fixTarotCard(window.TAROT_DATA.main.find(x=>x.id===Number(b.dataset.tarotCardId)));if(!card)return;const en=state.lang==='en',local=en?(card.en||{}):card,name=esc(local.name||card.name);document.querySelector('#cardDialogBody').innerHTML=`<div class="card-detail-layout"><div><img class="card-detail-image" src="${esc(cardImage(card))}" alt="${name}"></div><div><p class="muted">${en?'CRISTARIVA Divinatory Tarot':'Tarot divinatoire CRISTARIVA'} · ${en?'Card':'Carte'} ${String(card.id).padStart(2,'0')}</p><h2 id="cardDialogTitle">${name}</h2><p><b>${en?'Definition':'Définition'}</b><br>${esc(local.definition)}</p><p><b>${en?'Essential message':'Message essentiel'}</b><br>${esc(local.message)}</p><p><b>${en?'Keywords':'Mots-clés'}</b><br>${esc(local.keywords)}</p></div></div>`;const dialog=document.querySelector('#cardDialog');if(dialog&&!dialog.open)dialog.showModal()});
renderTarotCatalog();window.__CRISTARIVA_TAROT_READY__=true;window.CR_TAROT_INTEGRATION_VERSION='2026.09.22e';
})();
